<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Compose Email</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <div class="user-info">
            <span><%= user.full_name %></span>
            <a href="/logout">Sign out</a>
        </div>
    </header>
    <nav>
        <ul>
            <li><a href="/compose/<%= user.id %>">Compose</a></li>
            <li><a href="/inbox/<%= user.id %>">Inbox</a></li>
            <li><a href="/outbox/<%= user.id %>">Outbox</a></li>
        </ul>
    </nav>
    <main>
        <h1>Compose Email</h1>
        <% if (locals.error) { %>
            <p class="error"><%= error %></p>
        <% } %>
        <form action="/compose/<%= user.id %>" method="POST" enctype="multipart/form-data">
            <h3>Recipient Email:</h3>
            <input type="email" id="recipient" name="recipient" required placeholder="Enter recipient's email">
            
            <h3>Subject:</h3>
            <input type="text" id="subject" name="subject" placeholder="Enter subject">
            
            <h3>Message:</h3>
            <textarea id="body" name="body" placeholder="Enter your message"></textarea>
            
            <h3>Attachment:</h3>
            <input type="file" id="attachment" name="attachment">
            
            <button type="submit">Send</button>
        </form>
    </main>
    <script src="/js/email-actions.js"></script>
</body>
</html>
